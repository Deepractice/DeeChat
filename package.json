{
  "name": "deechat",
  "version": "1.0.0",
  "description": "DeeChat - Advanced AI Chat Application with DDD Architecture",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd app && npm run dev",
    "start": "cd app && npm start",
    "build": "npm run build:packages && cd app && npm run build",
    "build:packages": "npm run build:ai-config && npm run build:ai-chat && npm run build:mcp-client && npm run build:token-calculator",
    "build:ai-config": "cd packages/ai-config && npm run build",
    "build:ai-chat": "cd packages/ai-chat && npm run build",
    "build:mcp-client": "cd packages/mcp-client && npm run build",
    "build:token-calculator": "cd packages/token-calculator && npm run build",
    "clean": "cd app && npm run clean && cd ../packages/ai-config && npm run clean && cd ../ai-chat && npm run clean",
    "install:all": "npm install && cd app && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "DeeChat Team",
  "license": "MIT",
  "devDependencies": {
    "electron-rebuild": "^3.2.9",
    "rimraf": "^5.0.0",
    "typescript": "^5.2.0"
  }
}
